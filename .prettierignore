node_modules
.next
out
dist
build
pnpm-lock.yaml
uv.lock
.venv
packages/database/migrations
evals/reports

# Generated by the Supabase CLI, and git-ignored. Prettier reformatting it made
# `format:check` fail on a file that is not part of the repository.
supabase/.temp

# Verbatim third-party source. Reformatting it would break the CC BY-SA
# attribution claim that the text is unmodified, and could shift the byte
# offsets that evaluation anchors match against.
evals/datasets/*/source
